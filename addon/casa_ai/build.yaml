# Imagen base Debian y no Alpine: faster-whisper trae ctranslate2 como rueda
# manylinux (glibc), que en Alpine (musl) no instala.
build_from:
  amd64: ghcr.io/home-assistant/amd64-base-debian:bookworm
  aarch64: ghcr.io/home-assistant/aarch64-base-debian:bookworm
